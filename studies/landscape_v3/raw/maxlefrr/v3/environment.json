{
  "torch": "2.10.0+cu128",
  "cuda": "12.8",
  "n_gpu": 2,
  "gpus": [
    "Tesla T4",
    "Tesla T4"
  ],
  "inputs": "/kaggle/input/datasets/maxlefrr/landscape-v3-inputs",
  "data_root": "/kaggle/input/datasets/pankrzysiu/cifar10-python",
  "pilot": false,
  "n_tasks": 110,
  "protocol": {
    "lanczos_m_max": 400,
    "lanczos_check_every": 10,
    "lanczos_tol": 0.001,
    "lanczos_floor": 0.001,
    "reuse_tolerance_ce": 1e-05,
    "hess_fd_t": [
      1e-05,
      3e-05,
      0.0001,
      0.0003,
      0.001,
      0.003,
      0.01
    ],
    "_fd_t_note": "local dry run: r-normalised extreme eigendirections are far from quadratic already at t = 0.01 (top1 CE rise > 100 nats); steps reduced to 1e-5..1e-2 (float64)",
    "_chosen_from_local_pilot": "studies/landscape_v3/pilot/pilot_lanczos.json: plain seed 0, centre-frozen BN, train probe; ordinary and relative coordinates both met tol 1e-3 for top1/top2/min by 70 iterations with floor 1e-2; floor tightened to 1e-3 (scale = max(|theta|, 1e-3 |theta_top1|)); m_max 400 leaves >5x headroom"
  },
  "tf32": false,
  "cudnn_deterministic": true
}