# Change log

Values recomputed from the per-run `summary.json` records of every cell, using the configuration membership recorded in the experiment index.

- configurations: 165
- runs behind them: 377 (numerically valid: 374)
- configurations whose recomputed accuracy differs from the supplied index: 0

No corrections were needed: every configuration reproduces the supplied value.

The generic `Endpoint=target` column of the original table is deliberately not reproduced; it is unreliable for the early fixed-input-blur pilots. The inference state of each study is stated in its group header instead.
