% Transfer benchmark (owners: Aubin Charley, Alexandre Corrard).
% Fill ONLY from completed, verified runs; record run ids, commits, seeds and the
% generator command next to each filled row (paper/provenance/). Status codes:
%   n.y.a.      not yet available (no completed and verified run)
%   fail k/n    k of n runs crashed or reached a non-finite loss (kept visible)
%   n/a         the procedure has no defined insertion point for this architecture
% Rows below are organizational placeholders, not commitments.
\begin{table*}[t]
\caption{Reserved format for the transfer benchmark: final test accuracy (\%), mean $\pm$ sample SD over seeds, at the native filter-free endpoint. No transfer run has been completed and verified yet, so every entry reads n.y.a.\ (not yet available). When results arrive, \emph{fail $k/n$} will mark crashed or numerically diverged runs and \emph{n/a} a procedure without a defined insertion point for the architecture. Rows are placeholders, not commitments. The exploratory CIFAR-10 results of Appendix~\ref{app:exploration} selected the procedures on the test set and are not reused as independent confirmation. Each completed row will state seeds, optimizer settings, schedule scaling and insertion mapping (Appendix~\ref{app:transfer-mapping}).}
\label{tab:transfer}
\centering
\begin{tabular}{@{}lllcccc@{}}
\toprule
Dataset & Architecture & Optimizer & Plain & \methodR & \methodG & \methodRG\\
\midrule
CIFAR-10 & ResNet-20 & SGD & n.y.a. & n.y.a. & n.y.a. & n.y.a.\\
STL-10 & ResNet-20 & SGD & n.y.a. & n.y.a. & n.y.a. & n.y.a.\\
CIFAR-10 & ResNet-20 & AdamW & n.y.a. & n.y.a. & n.y.a. & n.y.a.\\
CIFAR-10 & Additional CNN (TBD) & SGD & n.y.a. & n.y.a. & n.y.a. & n.y.a.\\
\bottomrule
\end{tabular}
\end{table*}
