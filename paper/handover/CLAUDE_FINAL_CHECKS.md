# Final source checks for the revised paper

Please work from the attached `continuation_paper_revised` project and your full experiment repository. This is a focused verification and integration task. Preserve the English voice, the compact main text, the separation between experiment batches, and the exact executed method definitions. Do not launch training, new landscape sweeps, or new method comparisons for this task. Do not interrupt existing jobs. Use a separate worktree if the relevant worktree has ongoing experiments or manuscript edits.

The revised paper has already been compiled, its new benchmark and geometry tables recomputed from the supplied exports, and its figures rendered. Read `provenance/CLAIM_AUDIT.md` and `evidence/RECOMPUTED_CHECKS.json` first. Most requested work is complete; the following items need access to assets or state that was absent from the handoff.

1. **Check for manuscript edits newer than the supplied snapshot.** The rewrite starts from `manuscript@1bd1fe7`. Compare that with the latest author-provided local/Overleaf source if accessible. Preserve substantive newer author edits and report any unresolved textual conflict. Do not assume that remote HEAD is the author's latest Overleaf text.

2. **Verify the CIFAR-10 5k assets.** Inspect the actual subset and probe index arrays for both `stl_budget` and `reference_updates`, and their manifests. Report the number of unique training images, probe size, whether every probe index belongs to the training subset, reuse of the same subset between budgets and methods, and the relevant hashes. Confirm whether normalization constants are inherited from the 50k reference or recomputed on the 5k subset. The revised paper calls the CE a training-probe measurement and explicitly avoids certifying its currently unverified size. If the arrays establish the facts, replace that sentence in Appendix C with the exact verified sizes. Only change numerical tables if the underlying evaluated population was mislabeled; explain any such correction.

3. **Audit the provenance of Idriss's 28 runs on `continuation-core-experiments@4179584`.** Reconcile cell IDs, training seed, initial-weight hash, data-order hash, final-checkpoint hash and probe-file source. In particular, check the reference resolution accuracies 80.21 / 80.22 / 80.22, the Gaussian seed order 79.95 / 79.98 / 79.91, and the closely repeated combined accuracies. Check that no checkpoint, cached output or seed label was accidentally reused. The existing endpoint check is already complete: all 28 re-evaluated test accuracies agree with training logs and CE differences are below 2e-8. Repeating that scalar check alone does not establish checkpoint independence. Explain the narrow seed spread only if there is evidence; otherwise keep it descriptive and separate, as now. Do not merge these runs with the unified or landscape batches. If an identity error is found, identify precisely which cells and manuscript items are affected before correcting their labels or withdrawing those entries.

4. **Spot-check the historical assets against archived raw records.** The new compact catalogue preserves all 165 numeric rows of the supplied `tables/all_exploratory.tex`, including separate launches and failed configurations. The three historical figures and the selection/operator tables are carried from the supplied paper with the original provenance manifest. Using the archived commits, check the figures and quoted selection numbers, and the scope of final/target/fixed-inference records. The old generic `Endpoint=target` field is known to be unreliable for some early fixed-input-blur pilots and is deliberately omitted from the compiled catalogue. Do not reinstate it without deriving the true state from the run configuration. Keep the epoch budget, inference state, evaluation split and seed count attached to each study; do not rank unlike conditions as one experiment. Do not describe the retained uniform post-ReLU Gaussian as the best of every historical Gaussian variant: it won the simple placement comparison in the final paired batch.

5. **Confirm the numerical export-to-checkpoint links, without recomputing the study.** Check the hashes/manifests for the 20 landscape models, the 2k BN calibration set, 1k probes and 10k validation sets. The new manuscript treats the Hessian with frozen BN and finite sensitivity with pointwise recalibration as different functions. It also distinguishes the selected 268,336-weight Hessian from Idriss's all-parameter trace estimate. Preserve these distinctions. The temporal counterexample at Gaussian epoch 3 and the straight-segment barrier corrections in the handoff are already incorporated. There is no reason to rerun those measurements unless a specific mismatch is found.

The following material is intentionally absent and does not need to be produced for this verification: theory; unperformed input-derivative trajectories; an optimized mode-connecting path; unbacked gauge/displacement or PAC-Bayes figures; a Hessian differentiated through pointwise BN recalibration. CBS/SDPoint comparison status is “not started” in the supplied handoff. If those overnight jobs have since completed, report their status and provide a separate, fully sourced export before adding any comparative claim. Do not insert planned outcomes.

After verified corrections, run `python3 tools/build_assets.py` if data changed, compile with `latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex`, and inspect the affected PDF pages. Keep the official style files unchanged. Do not silently change paragraph spacing or shrink tables to fit. The paper can use a short red author note for a genuinely unresolved material fact, but do not fill it with speculative results.

Return at most four deliverables: (1) the corrected project ZIP, (2) the compiled PDF, (3) `FINAL_SOURCE_CHECKS.md` listing each check as verified / corrected / unresolved with code or asset paths and hashes, and (4) an optional ZIP containing only any newly completed, independently documented comparison results. No need to resend datasets, checkpoints or all raw landscape files. Include the exact text of any material changed numerical claim in the check report. Do not publish, merge or submit the paper as part of this task.
