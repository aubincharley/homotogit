These four files are vendored verbatim from PlotNeuralNet
(https://github.com/HarisIqbal88/PlotNeuralNet, MIT licence, LICENSE alongside).
They provide the `Box`, `RightBandedBox` and `Ball` tikz pics that
`../body.tex` draws with.  `../body.tex` itself is generated by
`scripts/fig_arch_plotneuralnet.py` -- edit the script, not the output.
