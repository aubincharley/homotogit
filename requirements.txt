# Pinned to the versions this study was executed with (see results/*/env.json for
# the exact resolved versions recorded at run time).
torch==2.5.1
torchvision==0.20.1
numpy==1.26.3
matplotlib==3.8.2
PyYAML==6.0.1
pytest==9.1.1
